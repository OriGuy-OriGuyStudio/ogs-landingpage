interface HeroSectionProps {}

const HeroSection = ({}: HeroSectionProps) => {
  return (
    <div className="">
      <p className="text-size-brand-p">Hero Section</p>
    </div>
  );
};

export default HeroSection;
